# Environment Variables for ChiliRig E-commerce
# Copy this file to .env.local and fill in your actual values

# API Authentication
# Generate secure random strings for production (e.g., using: openssl rand -hex 32)
API_KEY=your-api-key-here
ADMIN_KEY=your-admin-key-here

# Next.js Configuration
# NEXT_PUBLIC_* variables are exposed to the browser
# Use these for non-sensitive configuration

# Example: Analytics or other public API keys
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# Database (if you add a database later)
# DATABASE_URL=postgresql://user:password@localhost:5432/chilirig

# Email Service (if you add email functionality)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password

# Orders → Google Sheets (optional; if unset, orders are only logged to server console)
# Get this URL after deploying the Google Apps Script (see docs/GOOGLE_SHEETS_ORDERS.md)
# GOOGLE_SCRIPT_ORDERS_URL=https://script.google.com/macros/s/.../exec

# Pathao Courier (optional; creates a delivery order in Pathao when an order is placed)
# See docs/PATHAO_COURIER.md. Get store_id from Pathao merchant panel → Developer's API → Get Merchant Store Info.
# If a value contains # or = wrap it in double quotes (e.g. PATHAO_PASSWORD="my#pass").
# PATHAO_BASE_URL=https://courier-api-sandbox.pathao.com
# PATHAO_CLIENT_ID=
# PATHAO_CLIENT_SECRET=
# PATHAO_USERNAME=
# PATHAO_PASSWORD=
# PATHAO_STORE_ID=

# Payment Processing (if you add payment functionality)
# STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
API_KEY=
ADMIN_KEY=
ADMIN_PASSWORD=
